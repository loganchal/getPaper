name: Ewald dimension-9 classified scan

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The exhaustive dimension-9 scan completed successfully; automatic triggers are disabled."
